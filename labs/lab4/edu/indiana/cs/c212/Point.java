package edu.indiana.cs.c212;
//import java.lang.Math;

/**
 * In this file you will write the Point class. The point class has a constructor, and 2 class fields.
 * It also has a couple methods, as outlined below in comments. 
 * REMINDER: Make sure you complete all of them before trying to run the lab4 file.
 * Uncomment the line at the top of this file.
 * @author 
 * @author <PARTNERNAME>
 */
{ 
	//Every point is represented as an x and y value. Make some class variables to reflect that, and make them private. 

	
	/**
	 * Write a constructor that takes an x and y, and sets the class variables to those values
	 * REMINDER: Don't forget to use the this keyword 
	 * @param x
	 * @param y
	 */

	
	/**
	 * Write a getX() method that returns the class variable x
	 * REMINDER:Make sure your return-type matches the type of variable you're returning.
	 * @returns the class x variable
	 */

	
	/**
	 * Write a getX() method that returns the class variable y
	 * REMINDER:Make sure your return-type matches the type of variable you're returning.
	 * @returns the class y variable
	 */

	
	/**
	 * Write a distanceTo method that takes another Point called other and returns the distance from the current point to the given point.
	 * Remember Pythagoras' theorem. Use the MAth.sqrt and Math.pow functions to return the correct value.
	 * NOTE: Make sure you are being consistent with your references. if you subtract the horizontal distance between b and a, make sure you do the same with the vertical distance.
	 * REMINDER: Make sure your return types match the type you're calculating with.
	 * Link to Math package: http://docs.oracle.com/javase/6/docs/api/java/lang/Math.html
	 * Does this method need to be static or nonstatic? If you're unsure, ask your partner or ui to make sure you know.
	 * @param other Point 
	 * @return
	 */

	
	/**
	 * Write a distanceBetween method that takes two points and returns the distance between the two.
	 * Again remember Pythagoras. The difference between DistanceTo and DistanceBetween is a matter of referencing the correct object. 
	 * NOTE: Make sure you are being consistent with your references. If you subtract the horizontal distance between b and a, make sure you do the same with the vertical distance.
	 * REMINDER: Make sure your return types match the type you're calculating with.
	 * Link to Math package: http://docs.oracle.com/javase/6/docs/api/java/lang/Math.html
	 * Does this method need to be static or nonstatic? If you're unsure, ask your partner or ui to make sure you know.
	 * @param  Point p1
	 * @param  Point p2
	 * @return the distance between two points, given a starting and ending points
	 */

}
