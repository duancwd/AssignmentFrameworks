package edu.indiana.cs.c212;

import java.util.*;

public class Lab6 {
	
	//FIXME
	/**
	 * A directory is a list of people sorted in alphabetical order by their directory names.
	 * Write a method printDirectory() that takes a list of Person objects
	 * and prints out the directory.	
	 * One way to accomplish the sorting is to find the name to print first,
	 * print it, and then remove it. Repeat this process until the list is empty.
	 * You will learn better ways of sorting in the future.
	 * @param people A List of Person objects
	 */
	
	//FIXME
	/**
	 * Write a method getPeople() that prompts the user to enter 
	 * names on the keyboard. For each name entered, make a new Person with
	 * that name and add it to the list. If the name does not contain a space,
	 * ignore that name and prompt the user again for a name. Continue to prompt for names
	 * until the user enters "entry done". Return the list of names at this point. 
	 * @return A List of Person objects
	 */
	
	/**
	 * This main tests the methods of your Lab6 class.
	 * This method will not be graded, so feel free to add your 
	 * own tests.  Uncomment the lines of this method as you write 
	 * the methods it calls.
	 * @param args
	 */
	public static void main(String[] args) {
		//List<Person> people = getPeople();
        
        //printDirectory(people);
                
        /*List<Person> testPeople = new ArrayList<Person>();
        testPeople.add(new Person("Dexter Morgan"));
        testPeople.add(new Person("Walter White"));
        testPeople.add(new Person("Blair Waldorf"));
        testPeople.add(new Person("Lucrezia Borgia"));
        System.out.println("Printing directory: ");
        printDirectory(testPeople);
        System.out.println("Should print\nBorgia, L.\nMorgan, D.\nWaldorf, B.\nWhite, W.");
        */
	}

}
