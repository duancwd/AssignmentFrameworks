package edu.indiana.cs.c212;

import java.awt.Color;
import java.awt.Point;

/**
 * An object of this class represents a fruit that's dropped somewhere on a
 * Grid.
 * 
 * This class depends on class Grid.
 * 
 * You will be implementing the contents of this class.
 */
public class Fruit {
	//this constant is given to you as an example for later assignments/labs
	private static final Color FRUIT_COLOR = Color.RED;
	//FIXME
	/**
	 * please make two private fields, one of type Grid called grid and the other of type Point called point
	 */
	
	/**
	 * Write a public constructor that takes a grid and a point and sets the field values to the given values.
	 * @param grid
	 * @param point
	 */
	

	/**
	 * Write a public getPoint() method that returns the fruit's location as a Point
	 * @returns the fruit's location as a Point
	 */
	

	
	/**
	 * The draw method draws the fruit on the screen.
	 */
	public final void draw(){
		//FIXME uncomment when you add the private grid field
		//grid.turnOn(point, FRUIT_COLOR);
	}

	/**
	 * The undraw method removes the fruit from the screen.
	 */
	public final void undraw(){
		//FIXME uncomment when you add the private grid field
		//grid.turnOff(point);
	}
}
